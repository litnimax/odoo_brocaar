<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="netserver.list">
      <field name="name">netserver list</field>
      <field name="model">netserver.netserver</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="netserver.action_window">
      <field name="name">netserver window</field>
      <field name="res_model">netserver.netserver</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="netserver.action_server">
      <field name="name">netserver server</field>
      <field name="model_id" ref="model_netserver_netserver"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="netserver" id="netserver.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="netserver.menu_1" parent="netserver.menu_root"/>
    <menuitem name="Menu 2" id="netserver.menu_2" parent="netserver.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="netserver.menu_1_list" parent="netserver.menu_1"
              action="netserver.action_window"/>
    <menuitem name="Server to list" id="netserver" parent="netserver.menu_2"
              action="netserver.action_server"/>
    -->
  </data>
</odoo>