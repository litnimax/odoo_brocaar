<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="appserver.list">
      <field name="name">appserver list</field>
      <field name="model">appserver.appserver</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="appserver.action_window">
      <field name="name">appserver window</field>
      <field name="res_model">appserver.appserver</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="appserver.action_server">
      <field name="name">appserver server</field>
      <field name="model_id" ref="model_appserver_appserver"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="appserver" id="appserver.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="appserver.menu_1" parent="appserver.menu_root"/>
    <menuitem name="Menu 2" id="appserver.menu_2" parent="appserver.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="appserver.menu_1_list" parent="appserver.menu_1"
              action="appserver.action_window"/>
    <menuitem name="Server to list" id="appserver" parent="appserver.menu_2"
              action="appserver.action_server"/>
    -->
  </data>
</odoo>